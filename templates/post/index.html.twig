{% extends 'base.html.twig' %}

{% block title %}Post index{% endblock %}

{% block body %}
<div class="col-md-8">
    <h1 class="mt-5">All Posts</h1>

    {% for post in posts %}
        <div class="mt-5">
            <h2>{{ post.title }}</h2>
            <p class="text-muted">
                <span>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</span>
                <span>- <a href="{{ path('post_edit', {'id': post.id}) }}">Edit</a></span>
            </p>
            <p class="mt-4">
                {{ post.content }}
            </p>
            <a href="{{ path('post_show', {'id': post.id}) }}">Read more</a>
        </div>
    {% else %}
        <div class="mt-5">no records found</div>
    {% endfor %}
</div>

<div class="col-md-4">
    <div class="p-4 mt-5 border shadow-sm rounded">
        <h4 class="font-italic">About</h4>
        <p class="mb-0">Etiam porta <em>sem malesuada magna</em> mollis euismod. Cras mattis consectetur purus sit amet fermentum. Aenean lacinia bibendum nulla sed consectetur.</p>
        <a href="{{ path('post_new') }}" class="btn btn-outline-primary mt-3">Create</a>
    </div>
</div>
{% endblock %}
